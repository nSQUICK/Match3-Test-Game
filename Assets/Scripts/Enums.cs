public enum ShapeForm { Circle, Triangle, Square }
public enum BorderColor { Red, Green, Blue }
public enum AnimalType // actualy it's dota 2 heroes, but in example animals so its animal type
{
    Pudge,
    Chen,
    Tinker,
    Lina,
    Abadon
}
public enum SpecialEffect { None, Heavy, Sticky, Explode, Frozen }
